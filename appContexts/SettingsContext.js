/**
 * Declares and exports the settings context, which provides access to the user's settings data
 */

import { createContext } from 'react';

const SettingsContext = createContext({});

export default SettingsContext;
